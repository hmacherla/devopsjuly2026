name: WORKFLOW-1
on: workflow_dispatch

jobs:
  job-1:
   runs-on: ubuntu-24.04
    steps:
    - name: print hello world 
     run: echo "hello world"

     -name: print Thanks You
     run: "dhanyavad"
    
    
